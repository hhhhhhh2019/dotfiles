#!/usr/bin/bash

kitty @ ls > session.js

# FILENAME="$(date +"%Y/%d/%m/%H:%M:%S").kitty"
#
# kitty @ ls | python3 ./kitty-convert-dump.py > "$HOME/.local/state/$FILENAME"
#
# ln -sf "$HOME/.local/state/$FILENAME" "$HOME/.local/state/kitty/last"
